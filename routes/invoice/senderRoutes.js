const express = require('express');
const router = express.Router();
// const senderController = require('../controllers/invoice/senderController');
// const upload = require('../middleware/upload');

// router.get('/invoice/sender', senderController.getSenderProfile);
// router.post('/invoice/sender', upload.single('logo'), senderController.createSenderProfile);
// router.put('/invoice/sender', upload.single('logo'), senderController.updateSenderProfile);

module.exports = router;